# Core (required for rule-based scoring + hybrid predictor)
numpy>=1.24

# ML component (train.py, ml_model.py)
scikit-learn>=1.3
joblib>=1.3

# Optional: only needed if you wire up api.py
# fastapi>=0.100
# pydantic>=2.0

# Testing
pytest>=7.4
